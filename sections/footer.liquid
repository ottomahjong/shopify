<footer
  class="site-footer"
  data-section-id="{{ section.id }}"
>
  <div class="site-footer__inner container">
    <div class="site-footer__brand">
      <a href="{{ routes.root_url }}" class="site-footer__logo-link" aria-label="{{ shop.name }}">
        <span class="site-footer__wordmark">{{ section.settings.brand_name }}</span>
      </a>
    </div>

    {%- if section.settings.tagline != blank -%}
      <p class="site-footer__tagline">{{ section.settings.tagline }}</p>
    {%- endif -%}

    {%- if section.settings.copyright_text != blank -%}
      <p class="site-footer__copyright">
        {{- section.settings.copyright_text -}}
      </p>
    {%- endif -%}
  </div>
</footer>

{% schema %}
{
  "name": "Footer",
  "class": "section-footer",
  "settings": [
    {
      "type": "text",
      "id": "brand_name",
      "label": "Brand name",
      "default": "Otto"
    },
    {
      "type": "text",
      "id": "tagline",
      "label": "Tagline",
      "default": "One Table. Three Others."
    },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "Copyright text",
      "default": "© 2026 Otto Mahjong. All rights reserved."
    }
  ]
}
{% endschema %}
